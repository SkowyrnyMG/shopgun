import React from 'react';

import {{properCase name}} from '{{lowerCase directory}}{{#isContentType directory}}/{{camelCase componentType}}/{{else}}/{{/isContentType}}{{properCase name}}';
import { {{properCase name}}Props } from '{{lowerCase directory}}{{#isContentType directory}}/{{camelCase componentType}}/{{else}}/{{/isContentType}}{{properCase name}}/models.d';
import { render, screen{{#insertOption testFeatures "onClick"}}, userEvent{{/insertOption}} } from 'testUtils';

const props: {{properCase name}}Props = {

};

describe('{{properCase name}} - should pass all test cases', () => {
  test('the {{properCase name}} component renders correctly with required and optional props', () => {
    render(<{{properCase name}} {...props} />);

    const {{camelCase name}}Node = screen.getByTestId('test-{{dashCase name}}');

    // the component has been rendered
    expect({{camelCase name}}Node).toBeInTheDocument();

    // generating a snapshot
    expect({{camelCase name}}Node).toMatchSnapshot();
  });

  {{#insertOption testFeatures "onClick"}}
  test('after user click on {{properCase name}} correct action should be triggered', () => {
    render(<{{properCase name}} {...props} />);

    const {{camelCase name}}Node = screen.getByTestId('test-{{dashCase name}}');

    userEvent.click({{camelCase name}}Node);
  });
  {{/insertOption}}
});